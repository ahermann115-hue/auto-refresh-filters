name: Weekly Filter Update

on:
  schedule:
    - cron: '0 0 * * 0'  # Каждое воскресенье в 00:00 UTC
  workflow_dispatch:      # Разрешить ручной запуск

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      # ШАГ 1: Checkout с ВАШИМ токеном (это ключевое!)
      - name: 📥 Checkout with PAT
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.AUTO_UPDATE_TOKEN }}  # ТОЛЬКО ваш токен
          ref: main

      # ШАГ 2: Подготовка
      - name: 🔧 Setup
        run: |
          chmod +x update.sh
          echo "Токен установлен: ${{ secrets.AUTO_UPDATE_TOKEN != '' }}"
          echo "Рабочая директория: $(pwd)"
          ls -la

      # ШАГ 3: Запуск скрипта
      - name: 🚀 Run update script
        run: ./update.sh

      # ШАГ 4: Коммит и пуш с ТЕМ ЖЕ токеном
        - name: 📝 Commit and Push BLACKLIST ONLY
        env:
          GH_TOKEN: ${{ secrets.AUTO_UPDATE_TOKEN }}
        run: |
          # Настраиваем git
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # Сбрасываем ВСЕ изменения, кроме blacklist.txt
          git checkout -- update.sh whitelist.txt
          
          # Проверяем, изменился ли blacklist.txt
          if git diff --quiet blacklist.txt; then
            echo "ℹ️ Blacklist не изменился, пропускаем коммит"
          else
            echo "🔄 Blacklist изменился, коммитим..."
            git add blacklist.txt
            git commit -m "🤖 Автообновление blacklist $(date +'%Y-%m-%d %H:%M:%S')"
            
            # Пушим только blacklist
            git push "https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git" HEAD:main
            echo "✅ Успешно запушили blacklist!"
          fi
